---
apiVersion: v1
kind: Service
metadata:
  name: mystaysui
  namespace: beta-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: mystaysui